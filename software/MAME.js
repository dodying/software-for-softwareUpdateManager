'use strict';

const data = {
  site: {
    GitHub: 'https://github.com/mamedev/mame/releases/latest'
  },
  versionChoice: ['64bit.exe', /([\d.]+)/, false, 'name'],
  install: 'install'
};
module.exports = data;
