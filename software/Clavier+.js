'use strict';

const data = {
  site: {
    GitHub: 'https://github.com/guilryder/clavier-plus/releases/latest'
  },
  versionChoice: ['64.zip', /release([\d.]+)/],
  install: 'install'
};
module.exports = data;
