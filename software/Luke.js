'use strict';

const data = {
  site: {
    GitHub: 'https://github.com/DmitryKey/luke/releases/latest'
  },
  versionChoice: ['.zip$', /Luke ([\d.]+)/, false, 'name'],
  install: 'install'
};
module.exports = data;
