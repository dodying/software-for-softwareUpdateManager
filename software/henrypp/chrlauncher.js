'use strict';

const data = {
  site: {
    GitHub: 'https://github.com/henrypp/chrlauncher/releases/latest'
  },
  versionChoice: ['.zip$', /v.(.*)/],
  install: ['install', null, '*\\64']
};
module.exports = data;
